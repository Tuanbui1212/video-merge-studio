# Chỉ dùng khi chạy FE local (npm run dev:local) — KHÔNG dùng cho Docker build.
# Copy thành .env.local:
#   copy env.local.example .env.local

NEXT_PUBLIC_API_URL=http://127.0.0.1:8003
NEXT_PUBLIC_FRONTEND_PORT=3004
NEXT_PUBLIC_BACKEND_PORT=8003
NEXT_PUBLIC_DB_PORT=5433
